version: '3.8'

services:
  # PostgreSQL 데이터베이스
  postgres:
    image: postgres:16-alpine
    container_name: ziggle-chatbot-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ziggle_chatbot
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  # NestJS 애플리케이션
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ziggle-chatbot-app
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      # Database
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: ziggle_chatbot
      DB_SSL: false
      # Application
      PORT: 3000
      NODE_ENV: production
      # JWT
      # ⚠️ 프로덕션 배포 시 반드시 변경하세요 (최소 32자 이상)
      JWT_SECRET: your-production-jwt-secret-change-me-minimum-32-characters-required
      JWT_EXPIRES_IN: 3600
      # Admin
      # ⚠️ 프로덕션 배포 시 반드시 변경하세요 (최소 16자 이상)
      ADMIN_BEARER_TOKEN: your-production-admin-token-change-me-min-16
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "
        echo 'Waiting for database...' &&
        sleep 5 &&
        echo 'Running migrations...' &&
        bun run db:migrate &&
        echo 'Starting application...' &&
        bun run start:prod
      "

volumes:
  postgres_data:
